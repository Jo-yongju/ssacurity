# STM32 임베디드 개발 사전 검증 규칙

이 저장소에서 작업하는 에이전트는 단순 코드 작성자가 아니라 STM32 임베디드
시스템의 설계 검토자이자 구현자로 행동한다.

요청받은 내용을 곧바로 구현하지 않는다. 반드시 기존 프로젝트와 하드웨어
구성을 먼저 분석하고, 충돌 가능성·누락된 설정·부작용을 사전에 검증한 뒤
구현한다.

## 1. 구현 전 필수 조사

다음 자료를 먼저 직접 확인한다.

- `.ioc` 파일의 핀 설정과 Peripheral 설정
- 현재 사용 중인 GPIO, Timer, PWM Channel, UART, I2C, SPI, DMA
- 보드 내장 센서와 주변장치가 사용하는 핀
- `main.c`, `stm32f4xx_hal_msp.c`, 인터럽트 파일의 초기화 코드
- 기존 사용자 코드와 새 기능이 공유하는 전역 변수, HAL Handle, Timer
- Clock Tree와 각 Timer의 실제 입력 클럭
- FreeRTOS Task, Queue, Semaphore, Interrupt Priority 설정
- 프로젝트에 포함된 보드 매뉴얼, 회로도, 핀맵 문서

파일과 설정을 직접 확인하지 않은 상태에서 핀이 비어 있다고 추측하지 않는다.

## 2. 자원 충돌 검사

구현 전에 최소한 다음 항목을 검사한다.

### GPIO 및 핀

- 해당 핀이 이미 다른 Peripheral이나 내장 센서에 사용되고 있는가?
- Alternate Function 번호가 올바른가?
- 동일한 핀을 GPIO와 Peripheral이 중복 사용하고 있지 않은가?
- 입력·출력 방향과 Pull-up/Pull-down 설정이 적절한가?
- 연결 장치의 논리 전압이 STM32 핀 전압과 호환되는가?

### Timer 및 PWM

- Timer와 Channel이 기존 PWM, Encoder, 제어주기 Timer와 충돌하지 않는가?
- 해당 핀이 원하는 Timer Channel을 실제로 지원하는가?
- PWM 주파수와 Duty 계산이 Clock Tree 설정과 일치하는가?
- Timer 변경이 다른 모터, 엔코더 또는 RTOS Tick에 영향을 주지 않는가?

### 통신 및 인터럽트

- UART, SPI, I2C, DMA Stream 및 Channel이 중복되지 않는가?
- 인터럽트 우선순위가 FreeRTOS 규칙을 위반하지 않는가?
- Callback 함수가 기존 코드와 충돌하지 않는가?
- 통신 속도와 핀 연결 방향이 올바른가?

### 소프트웨어 구조

- CubeMX가 다시 코드를 생성해도 수정사항이 보존되는가?
- 사용자 코드는 `USER CODE BEGIN/END` 영역에 들어가는가?
- 기존 함수, 전역 변수, 구조체 이름과 충돌하지 않는가?
- 초기화 순서 때문에 장치가 잘못 동작할 가능성이 없는가?

## 3. 구현 전 검증 보고

코드를 수정하기 전에 다음 형식으로 검증 결과를 먼저 작성한다.

### 현재 자원 사용 현황

| 자원 | 현재 용도 | 새 기능 용도 | 충돌 여부 | 근거 |
| --- | --- | --- | --- | --- |
| 예: PA1 | 내장 센서 인터럽트 | LPWM | 충돌 | `.ioc` 및 보드 회로도 |
| 예: PA3 | 미사용 | LPWM | 사용 가능 | `.ioc` 확인 |

### 발견된 위험

| 위험도 | 문제 | 발생 가능한 증상 | 해결 방법 |
| --- | --- | --- | --- |
| 높음 | 핀 중복 사용 | PWM 출력 실패 또는 센서 오동작 | 대체 핀 사용 |
| 중간 | Timer 주파수 오류 | PWM 주파수 불일치 | Prescaler/Period 재계산 |

위험도 기준:

- **높음**: 부품 손상, 핀 충돌, Peripheral 동작 불가
- **중간**: 간헐적 오동작, 타이밍 문제, RTOS 충돌
- **낮음**: 유지보수성, 코드 구조, 경고 발생 가능성

각 판단에는 반드시 확인한 파일명, 설정값 또는 문서 근거를 제시한다. 근거가
없는 내용은 사실처럼 단정하지 않고 `확인 필요`로 표시한다.

이 구현 전 보고는 실제 코드 또는 설정을 수정하기 전에 사용자에게 먼저
제시한다.

## 4. 구현 판단 규칙

- 치명적인 충돌이 없으면 가장 안전한 설계를 선택하여 구현한다.
- 충돌이 발견되면 기존 설계를 그대로 구현하지 않는다.
- 사용 가능한 대체 핀, Timer, Channel을 조사하고 변경안을 제시한다.
- 대안이 여러 개라면 기존 기능에 미치는 영향이 가장 작은 안을 선택한다.
- 핀 하나만 바꾸는 것으로 끝내지 않고 `.ioc`, 초기화 코드, HAL Handle,
  실제 배선 변경까지 함께 반영한다.
- 기존 기능을 제거하거나 변경해야 할 경우 변경 이유와 영향 범위를 기록한다.

### 과잉 보수 방지 및 최소 설계 원칙

안전 판단은 막연한 우려가 아니라 정확한 부품, 패키지, 핀, 동작 모드의
데이터시트와 보드 회로도에 근거한다. 정격과 보장 동작 조건 안에서 직접
연결이나 단순 구현이 가능하면 그것을 기본안으로 선택한다.

- 특정 MCU 계열에 대한 일반론보다 현재 사용하는 정확한 MCU, 패키지, 핀의
  I/O 구조와 전기적 특성을 우선 확인한다.
- 안전 대책을 `필수`, `권장`, `선택`으로 명확히 구분한다. 정격 초과나 명세
  위반 근거가 없으면 선택 사항을 필수처럼 설명하지 않는다.
- 데이터시트가 직접 연결을 허용하면 직접 연결을 우선 제안한다. 분압기,
  레벨시프터, 직렬 저항, Pull-up/Pull-down, RC 필터 등 추가 부품은 필요한
  수치 근거와 효과가 있을 때만 요구한다.
- 소프트웨어의 추가 필터, 상태, 타임아웃, 래치, Watchdog도 요구사항 또는
  확인된 고장 위험에 필요한 최소 범위로 제한한다.
- 먼저 사용자가 요청한 테스트를 만족하는 최소 회로와 최소 기능을 구현한다.
  실제 증상이나 정량적으로 확인된 위험이 있을 때만 보호 기능을 확장한다.
- 보호 대책을 제안할 때는 얻는 효과뿐 아니라 배선, 부품 수, 디버깅 및
  유지보수 복잡도 증가도 함께 설명한다.
- 이전 판단이 지나치게 보수적이었다는 근거가 확인되면 즉시 정정하고 관련
  설계, 문서 및 테스트 절차도 더 단순한 안으로 맞춘다.

현재 프로젝트의 HC-SR04 ECHO 입력은 다음과 같이 판단한다.

- STM32F429ZI의 PB3는 공식 데이터시트상 `FT`(5 V tolerant) I/O이고 현재
  `TIM2_CH2` 디지털 입력으로 사용한다.
- STM32와 HC-SR04가 함께 전원 공급되고 공통 GND가 연결된 조건에서는
  HC-SR04 ECHO를 PB3에 직접 연결할 수 있다.
- 이 조건에서 10 kΩ/20 kΩ 분압기는 추가 보호를 원하는 경우의 선택 사항이지
  필수 사항이 아니다.
- 이 판단을 다른 MCU, 다른 핀, Analog 모드 또는 STM32가 꺼진 상태에
  일반화하지 않는다.

## 5. 구현 후 검증

구현 후 다음을 수행한다.

1. 프로젝트 빌드
2. 컴파일 오류와 경고 확인
3. `.ioc`의 핀 충돌 및 CubeMX 경고 확인
4. 수정된 Peripheral 초기화 값 확인
5. 기존 기능에 영향을 주는 코드 변경 여부 확인
6. 실제 하드웨어 없이 확인 가능한 정적 검증 수행
7. 실제 보드에서 확인할 테스트 절차 작성
8. 정상 동작 기준과 실패 증상을 명확히 정의

## 6. 최종 보고 순서

최종 답변은 다음 순서로 작성한다.

1. 요청사항 요약
2. 기존 프로젝트 분석 결과
3. 발견된 충돌 및 위험
4. 선택한 설계와 선택 이유
5. 수정한 파일 목록
6. 핵심 코드 변경 내용
7. 빌드 및 정적 검증 결과
8. 실제 보드 테스트 절차
9. 남아 있는 미검증 사항

## 절대 금지 사항

- 핀 사용 현황을 확인하지 않고 임의로 핀을 선택하는 것
- `.ioc`와 실제 코드를 따로 수정해 설정이 불일치하게 만드는 것
- 기존 Peripheral 설정을 이유 없이 덮어쓰는 것
- 데이터시트나 프로젝트 파일을 확인하지 않고 “사용 가능하다”고 단정하는 것
- 빌드 성공만으로 하드웨어 동작이 검증됐다고 판단하는 것
